# Domain layer

Framework-free entities, value objects, domain services, events, and port protocols. This layer must not import `application`, `adapters`, FastAPI, Pydantic transport models, database clients, or HTTP clients.
