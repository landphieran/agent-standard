# Dependencies / build
node_modules/
dist/
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.vitest/
.next/
playwright-report/
test-results/

# Env / local
.env
.env.*
!.env.example
{% if 'claude' in agents -%}
# Personal agent settings — never commit
.claude/settings.local.json
{% endif -%}

# Generated build SBOMs are CI artifacts. The canonical repository SBOM(s)
# (`bom.cdx.json` / `bom.spdx.json`) are intentionally tracked.
build.bom.*.json
release.bom.*.json
